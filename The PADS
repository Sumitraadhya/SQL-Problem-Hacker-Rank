SELECT CONCAT(Name, '(', SUBSTR(OCCUPATION, 1, 1), ')') as name FROM OCCUPATIONS ORDER BY name; 
SELECT CONCAT('There are a total of', ' ', COUNT('OCCUPATION'), ' ',
lower(OCCUPATION), 's.') as Profession FROM OCCUPATIONS
GROUP BY OCCUPATION 
ORDER BY PROFESSION
;
